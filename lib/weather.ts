import "server-only";
export type WeatherData={locationName:string;current:{temperature:number;apparentTemperature:number;weatherCode:number;humidity:number;precipitation:number;windSpeed:number;windGusts:number};days:Array<{date:string;weatherCode:number;minTemperature:number;maxTemperature:number;precipitationProbability:number;precipitation:number;windGusts:number;et0:number|null;soilMoisture:number|null}>};
const number=(name:string)=>{const value=Number(process.env[name]);return Number.isFinite(value)?value:null}; const finite=(value:unknown):value is number=>typeof value==="number"&&Number.isFinite(value);
export async function getFarmWeather():Promise<{kind:"ready";data:WeatherData}|{kind:"unconfigured"}|{kind:"unavailable"}>{const latitude=number("FARM_LATITUDE"),longitude=number("FARM_LONGITUDE"),timezone=process.env.FARM_TIMEZONE??"Africa/Casablanca";if(latitude===null||longitude===null||latitude<-90||latitude>90||longitude<-180||longitude>180)return{kind:"unconfigured"};try{const url=new URL("https://api.open-meteo.com/v1/forecast");url.search=new URLSearchParams({latitude:String(latitude),longitude:String(longitude),timezone,forecast_days:"7",current:"temperature_2m,apparent_temperature,weather_code,relative_humidity_2m,precipitation,wind_speed_10m,wind_gusts_10m",daily:"weather_code,temperature_2m_min,temperature_2m_max,precipitation_probability_max,precipitation_sum,wind_gusts_10m_max,et0_fao_evapotranspiration",hourly:"soil_moisture_0_to_1cm"}).toString();const response=await fetch(url,{next:{revalidate:1800},signal:AbortSignal.timeout(8000)});if(!response.ok){const providerText=await response.text();console.error("Open-Meteo forecast error",{status:response.status,providerText:providerText.slice(0,500)});return{kind:"unavailable"}}const raw=await response.json() as {current:Record<string,number>;daily:Record<string,Array<number|string|null>>;hourly:{time:string[];soil_moisture_0_to_1cm:Array<number|null>}};const moistureByDay=new Map<string,number[]>();raw.hourly.time.forEach((time,index)=>{const value=raw.hourly.soil_moisture_0_to_1cm[index];if(finite(value)){const day=time.slice(0,10);moistureByDay.set(day,[...(moistureByDay.get(day)??[]),value])}});return{kind:"ready",data:{locationName:process.env.FARM_LOCATION_NAME||"Sidi Bettach",current:{temperature:raw.current.temperature_2m,apparentTemperature:raw.current.apparent_temperature,weatherCode:raw.current.weather_code,humidity:raw.current.relative_humidity_2m,precipitation:raw.current.precipitation,windSpeed:raw.current.wind_speed_10m,windGusts:raw.current.wind_gusts_10m},days:raw.daily.time.map((date,index)=>{const values=moistureByDay.get(String(date))??[];return{date:String(date),weatherCode:Number(raw.daily.weather_code[index]),minTemperature:Number(raw.daily.temperature_2m_min[index]),maxTemperature:Number(raw.daily.temperature_2m_max[index]),precipitationProbability:Number(raw.daily.precipitation_probability_max[index]),precipitation:Number(raw.daily.precipitation_sum[index]),windGusts:Number(raw.daily.wind_gusts_10m_max[index]),et0:finite(raw.daily.et0_fao_evapotranspiration[index])?Number(raw.daily.et0_fao_evapotranspiration[index]):null,soilMoisture:values.length?values.reduce((sum,value)=>sum+value,0)/values.length:null}})}}}catch(error){console.error("Open-Meteo forecast request failed",error instanceof Error?error.message:error);return{kind:"unavailable"}}}
